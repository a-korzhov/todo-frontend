import styled from "styled-components";
import StyledWrapper from "./StyledWrapper";

const StyledYellowWrapper = styled(StyledWrapper)`
  background-color: #fdd835;
`;

export default StyledYellowWrapper;